$header
-module($name_sup).
-behaviour(supervisor).

-export([start_link/1]).
-export([init/1]).

start_link(Args) -> supervisor:start_link(?MODULE, Args).

init(Args) -> {ok, {{one_for_one, 1, 10}, [
	{$name, {$name_server, start_link, [Args]},
	permanent, 10, worker, [$name_server]}
]}}.
